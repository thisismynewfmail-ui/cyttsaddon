Flask>=3.0
Flask-SocketIO>=5.3
simple-websocket>=1.0
requests>=2.31
# optional: more accurate token counts (a char-ratio fallback is used otherwise)
# tiktoken>=0.5
# optional: neural speech output (the SPEECH tab falls back to the noise engine
# when this is not installed)
# piper-tts>=1.2
